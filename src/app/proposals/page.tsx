import PlaceholderPage from "@/components/PlaceholderPage";

export default function ProposalsPage() {
  return (
    <PlaceholderPage
      title="Proposals"
      active="Proposals"
      description="This section will eventually track proposals, proposal statuses, signed jobs, and customer-facing documents."
      currentWorkaround="For now, proposals are managed from inside each individual estimate."
    />
  );
}