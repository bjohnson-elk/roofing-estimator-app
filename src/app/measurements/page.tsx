import PlaceholderPage from "@/components/PlaceholderPage";

export default function MeasurementsPage() {
  return (
    <PlaceholderPage
      title="Measurements"
      active="Measurements"
      description="This section will eventually track measurement orders, uploaded reports, measurement extraction, and pending measurement tasks."
      currentWorkaround="For now, measurements are managed from inside each individual estimate."
    />
  );
}