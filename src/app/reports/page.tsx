import PlaceholderPage from "@/components/PlaceholderPage";

export default function ReportsPage() {
  return (
    <PlaceholderPage
      title="Reports"
      active="Reports"
      description="This section will eventually show sales, margin, cost, production, package, and user performance reporting."
      currentWorkaround="For now, reporting is not active. Estimate totals and margins are reviewed inside each estimate."
    />
  );
}