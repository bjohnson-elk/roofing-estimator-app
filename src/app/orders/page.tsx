import PlaceholderPage from "@/components/PlaceholderPage";

export default function OrdersPage() {
  return (
    <PlaceholderPage
      title="Orders"
      active="Orders"
      description="This section will eventually show material orders, labor orders, production status, archived jobs, and order history."
      currentWorkaround="For now, orders are managed from inside each signed estimate."
    />
  );
}