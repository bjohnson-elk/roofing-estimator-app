import PlaceholderPage from "@/components/PlaceholderPage";

export default function CustomersPage() {
  return (
    <PlaceholderPage
      title="Customers"
      active="Customers"
      description="This section will eventually show customer records, contact information, job history, and related estimates."
      currentWorkaround="For now, customer information is entered and managed directly inside each estimate."
    />
  );
}